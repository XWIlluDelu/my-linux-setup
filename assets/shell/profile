# POSIX login shell (bash, dash): set PATH and environment.
. "$HOME/.config/shell/env.sh"

# Load bash interactive settings for login shells.
if [ -n "${BASH_VERSION:-}" ] && [ -f "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc"
fi
